class Query {
  constructor(log) {
    this.log = log;
  }

  getTopAgent(data) {
    //return top agent + number of uses
  }

  getTopRequest(date) {
    //return request verb and path + number of uses
  }

}

module.exports = Query;
